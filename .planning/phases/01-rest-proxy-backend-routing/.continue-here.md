---
context: phase
phase: 01-rest-proxy-backend-routing
activity: secure-phase (threat verification)
paused_at: "Step 4 — Present Threat Plan (awaiting user choice before gsd-security-auditor spawn)"
status: paused
last_updated: "2026-04-17T21:05:00Z"
---

# Resume: `/gsd-secure-phase 1`

<current_state>
Paused mid-workflow inside `/gsd-secure-phase 1` at Step 4. The full threat
register has been extracted from the three PLAN.md files and summarized below.
The user was about to pick an option when they paused to lower Cursor's
reasoning effort and clear context (this verification work is mechanical
grep-chasing — doesn't need high reasoning budget).

Phase 01 itself is code-complete and clean. No uncommitted work. Three new
commits ahead of `github/feature/dashboard-studio-tile-proxy-v2` (not yet
pushed): 8afd921, d45b39c, 3b4dc25.
</current_state>

## Threat Register (ready for verification)

**Totals: 24 threats · 21 `mitigate` · 3 `accept` · 0 `## Threat Flags` in SUMMARYs**

### Plan 01-01 (10 threats)
| ID | Category | Disposition | Mitigation handle |
|---|---|---|---|
| T1-01-SSRF | Tampering/InfoDisclosure | mitigate | `_validate_url` 4-layer (scheme/inject/host/IP) |
| T1-02-InjectionZXY | Tampering | mitigate | `_check_zxy_value` / `_resolve_tile` |
| T1-03-ResponseSize | DoS | mitigate | `MAX_TILE_BYTES` + `resp.read(N+1)` |
| T1-04-Redirect | Tampering | mitigate | `_NoRedirectHandler` in `_opener` |
| T1-05-CachePoisoning | Tampering | mitigate | `_make_cache_key` = sha256 full hex |
| T1-07-RateLimitAmp | DoS | **accept** | Indirectly mitigated by LRU cache |
| T1-08-UnauthAccess | Spoofing | mitigate | `requireAuthentication = true` (set in 01-02) |
| T1-09-Disabled | Misuse | mitigate | `handle_GET` step 2 → 503 `proxy_disabled` |
| T1-10-BinaryEncoding | Integrity | mitigate | `response.write(bytes)` pass-through |
| T1-11-ErrorLeak | InfoDisclosure | mitigate | `_write_json_error` — short codes only |

### Plan 01-02 (6 threats)
| ID | Category | Disposition | Mitigation handle |
|---|---|---|---|
| T2-01-AuthBypass | Spoofing | mitigate | `requireAuthentication = true` in restmap.conf |
| T2-02-WrongStanza | Config | mitigate | `[script:maps_plus_tile_proxy]` header |
| T2-03-Python2Fallback | Tampering | mitigate | `python.version = python3` in restmap.conf |
| T2-04-AllowlistWeakening | Tampering | **accept** | local/ edits intended; IP-block defense remains |
| T2-05-MissingFromPackage | Availability | mitigate | `build_release.sh` fail-fast checks bin/ present |
| T2-06-SystemFileWrite | Integrity | mitigate | All writes scoped under app `default/` |

### Plan 01-03 (8 threats)
| ID | Category | Disposition | Mitigation handle |
|---|---|---|---|
| T3-01-PathTraversal | Tampering | mitigate | `_assert_within_cache_dir` (realpath prefix) |
| T3-02-TornWrite | Integrity | mitigate | `tempfile.mkstemp` + `os.replace` |
| T3-03-ConcurrencyRace | Integrity | mitigate | `threading.Lock` around set/prune |
| T3-04-DiskExhaustion | DoS | mitigate | `_prune_locked` enforces `max_bytes` |
| T3-05-CloudFilesystem | Availability | mitigate | `PermissionError`/`EROFS` → `enabled=False` |
| T3-06-SerializationFormat | Integrity | mitigate | `MP01` length-prefixed binary; no pickle |
| T3-07-SymlinkEscape | Tampering | mitigate | `realpath` before prefix check |
| T3-08-MtimeUpdateRace | Reliability | **accept** | `os.utime` best-effort, racing is benign |

### Accepted-risk rationales (go straight into SECURITY.md)

- **T1-07-RateLimitAmp** (DoS): Per-session rate limiting deferred (D-05/D-06). Mitigated indirectly by LRU cache reducing upstream load.
- **T2-04-AllowlistWeakening** (Tampering): Users editing `local/settings.json` is an intended operation (D-01). FS permissions + AppCert scope protect against unauthorized edits. Defense-in-depth: `_validate_url` IP-block (T1-01 layer 4) still blocks private IPs even if someone adds a private IP to the allowlist.
- **T3-08-MtimeUpdateRace** (Reliability): `os.utime()` touch-on-read is best-effort. Two concurrent reads racing on `utime` is benign (both set near-current time). No lock needed for `utime`.

## Required Reading (in order)

1. `.planning/phases/01-rest-proxy-backend-routing/01-01-PLAN.md` (threat_model block lines 158-181) — Plan 01-01 threats
2. `.planning/phases/01-rest-proxy-backend-routing/01-02-PLAN.md` (threat_model block lines 122-140) — Plan 01-02 threats
3. `.planning/phases/01-rest-proxy-backend-routing/01-03-PLAN.md` (threat_model block lines 147-168) — Plan 01-03 threats
4. `.planning/phases/01-rest-proxy-backend-routing/01-01-SUMMARY.md` (table "Threat Mitigations Implemented" ~line 126) — executor-reported coverage
5. `.planning/phases/01-rest-proxy-backend-routing/01-02-SUMMARY.md` (table ~line 130)
6. `.planning/phases/01-rest-proxy-backend-routing/01-03-SUMMARY.md` (table ~line 105)
7. `$HOME/.cursor/agents/gsd-security-auditor.md` — auditor prompt
8. `$HOME/.cursor/get-shit-done/templates/SECURITY.md` — output template

<completed_work>

In this session, before the pause:

1. **Fixed the one open UAT gap**: HTTP 400 → 403 for SSRF policy rejections. Split `_validate_url` codes into policy vs syntax classes in `bin/tile_proxy.py` handle_GET step 5 via `_SSRF_POLICY_CODES` set. Commit: `3b4dc25`.
2. **Added 3 integration tests** covering the new 403 mapping. Full suite: 77/77 pass.
3. **Re-ran UAT Tests 4 and 5** against the live splunkd-10.2.0 container — both now return HTTP 403 with sanitized JSON bodies. Regression check (valid OSM tile) still returns 200 image/png 6924B.
4. **Updated UAT artifacts**: `01-UAT.md` now shows 8/8 pass, 0 issues; Gap #3 flipped `failed → fixed`. `STATE.md` and `HANDOFF.json` updated accordingly.
5. **Kicked off `/gsd-secure-phase 1`** — detected State B (no SECURITY.md yet, all PLANs/SUMMARYs present), extracted the 24-threat register above, confirmed 0 `## Threat Flags` in SUMMARYs, presented the register and verify/accept/cancel options to the user.
</completed_work>

<remaining_work>

- **Step 4 (in progress, waiting on user)**: User picks option 1 (verify via auditor), 2 (accept-all), or 3 (cancel).
- **Step 5**: Spawn `gsd-security-auditor` subagent with required_reading and threat_register context. Subagent type should be `gsd-security-auditor` per workflow (mapped to Cursor's Task tool with `generalPurpose` subagent_type if gsd-security-auditor isn't an available enum value — read the skill adapter).
- **Step 6**: Write `.planning/phases/01-rest-proxy-backend-routing/01-SECURITY.md` from the template. Include threat register table with statuses, accepted-risks log (3 entries above), audit trail row.
- **Step 7**: Commit `docs(phase-01): add security threat verification`.
- **Step 8**: If `threats_open: 0`, emit GSD-style success banner + route to `/gsd-validate-phase 1` or `/gsd-verify-work 1`.
</remaining_work>

<decisions_made>

- **HTTP 403 vs 400 status-code split for SSRF (this session, commit `3b4dc25`)**: Encoded as `_SSRF_POLICY_CODES = {"host_not_allowed", "invalid_ip", "private_ip_blocked"}` in `bin/tile_proxy.py` handle_GET step 5. Client-malformed codes (`scheme_not_https`, `invalid_chars`, `dns_failed`) remain 400. Justification: RFC 9110 §15.5.4.
- **UAT fix deployed via `docker cp` rather than tarball reinstall**: The scripttype=python REST handler spawns a fresh python subprocess per request, so file changes are live without splunkd restart. Full reinstall hung during the earlier restart attempt (splunkd shut down cleanly but didn't auto-restart; had to manually start). The docker-cp shortcut is acceptable for UAT verification of the logic change; any "clean install" regression check can be done on resume by rebuilding the tarball.
</decisions_made>

<blockers>

None. This is a clean user-initiated pause, not a technical block.
</blockers>

## Infrastructure State

- **Splunk container**: `splunk-10-dev-3` (splunk/splunk:10.2.0) — running, healthy. Ports 8000 (web http) and 8089 (mgmt https). Admin password `A00mast3r`. Installed app: `leaflet_maps_app` v4.6.1. Container's `bin/tile_proxy.py` md5 = `17166d088adb85e379943261f1842598` (matches host working tree, which matches commit 3b4dc25).
- **Node env**: pinned to 24.14.0 via `appserver/static/visualizations/maps-plus/.nvmrc`. Activate with `eval "$(fnm env --shell zsh)" && fnm use 24.14.0`.
- **Git**: branch `feature/dashboard-studio-tile-proxy-v2`, 3 commits ahead of `github/feature/dashboard-studio-tile-proxy-v2` (not pushed). Working tree clean (one unrelated untracked file in `docs/superpowers/specs/`).

## Critical Anti-Patterns (do NOT repeat)

None discovered this session. The 403 fix and UAT close-out went smoothly.

<context>
The secure-phase verification should be mechanical: each SUMMARY's
"Threat Mitigations Implemented (grep-verifiable)" table was explicitly
written to name function names / constants that the auditor can grep for.
The three accept threats have documented rationales. Expected outcome:
`## SECURED` return with threats_closed = 24/24, threats_open = 0, then
straight through to Step 6 (write SECURITY.md) → Step 7 (commit) →
Step 8 (route to /gsd-validate-phase 1 or /gsd-verify-work 1).

User paused specifically to lower reasoning effort before the auditor runs.
This is a budget optimization, not a course correction.
</context>

<next_action>
Re-invoke `/gsd-secure-phase 1`. It will detect State B again (no SECURITY.md
yet), re-extract the same 24-threat register, and land at Step 4. Pick
Option 1 (verify via auditor) and proceed through Steps 5-8.

The handoff data is already pre-computed in `.planning/HANDOFF.json` under
`threat_register` — the resuming agent can use this rather than re-parsing
the PLAN files if it wants to save tokens.
</next_action>
