---
phase: 03-ds-parent-frame-auth-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - appserver/static/visualizations/maps-plus/package.json
  - appserver/static/visualizations/maps-plus/src/ds-tile-proxy-helpers.js
  - appserver/static/visualizations/maps-plus/src/maps-plus.js
  - appserver/static/visualizations/maps-plus/tests/parent-bridge-drift.test.js
  - appserver/static/visualizations/maps-plus/tests/parent-bridge-rpc-iframe.test.js
  - appserver/static/visualizations/maps-plus/tests/parent-bridge-shim.test.js
  - appserver/static/visualizations/maps-plus/tests/parent-bridge-blob-lifecycle.test.js
  - appserver/static/visualizations/maps-plus/tests/ds-proxy-tile-layer.test.js
  - appserver/static/visualizations/maps-plus/visualization.js
  - appserver/static/visualizations/maps-plus/visualization.css
autonomous: false
requirements:
  - DS-AUTH-01
  - DS-AUTH-02
  - DS-AUTH-03
  - DS-AUTH-04
  - DS-AUTH-05
  - DS-AUTH-06
must_haves:
  truths:
    - "`DsProxyTileLayer` in DS mode implements `createTile` that `postMessage`es `{ type: 'maps-plus:fetch-tile', requestId, url, z, x, y, s?, r? }` to `window.top` with target origin exactly `_SPLUNK_ORIGIN` from `_detectSplunkOrigin()` (never `'*'`); if `_SPLUNK_ORIGIN === ''`, latch module `_bridgeLatchThisIframe` to `'absent'` immediately with the D-NN-6 warning (module-global log guard) and blank tiles"
    - "`tileData` arrives as a structured-clone `Blob`; iframe uses `URL.createObjectURL` then `img.src`; `URL.revokeObjectURL` runs on **both** `img.onload` and `img.onerror` (D-03)"
    - "D-02: the **1500** ms bridge-presence timer arms **only** after a real `window.top.postMessage` send (first sent message when module latch `_bridgeLatchThisIframe === 'unknown'`), i.e. **after** both `PROXY_URL_SHAPE.test(url)` and inflight-cap checks pass — never armed on URL-shape reject or inflight-cap blank path; on timer fire latch absent with warn exactly once via module `_bridgeAbsentLoggedThisIframe`"
    - "D-06: monotonic integer `requestId` per layer instance starting at **1**; **8000** ms per-request timer; **256** concurrent in-flight postMessage requests max — at cap render blank tile **without** postMessage (no console log); first timeout logs `[maps-plus:ds-proxy] tile timeout (further suppressed until next page load)` **once per iframe lifetime** via module `_timeoutLoggedThisIframe`, then subsequent timeouts suppress (same iframe-global scope as D-NN-6 absent-warning latch)"
    - "Inbound handler accepts only `data.type === 'maps-plus:tile-result'`; rejects `event.origin` not exactly equal to `_SPLUNK_ORIGIN` (D-NN-4); Jest includes close-but-not-equal negative strings (trailing slash, port `:8443`, `https://splunkweb-evil.example.com` vs `https://splunkweb.example.com`)"
    - "Grep of `appserver/static/parent-auth-bridge.js` plus `appserver/static/visualizations/maps-plus/src/maps-plus.js` finds **only** these four `type` string literals total: `maps-plus:fetch-tile`, `maps-plus:tile-result`, `maps-plus:ping`, `maps-plus:pong` — iframe never emits `ping`/`pong` (DS-AUTH-02, D-NN-2)"
    - "`npm test` passes; `npm run build` + committed `visualization.js` and `visualization.css` (D-22)"
    - "Manual UAT-2..UAT-7 recorded in `03-02-SUMMARY.md` against `splunk-10-dev`"
  artifacts:
    - path: "appserver/static/visualizations/maps-plus/src/maps-plus.js"
      provides: "DsProxyTileLayer.createTile + message listener + onRemove cleanup"
      contains: "createTile"
    - path: "appserver/static/visualizations/maps-plus/tests/parent-bridge-drift.test.js"
      provides: "BEGIN/END marker drift guard"
      contains: "BEGIN_PROXY_URL_SHAPE"
  key_links:
    - from: "DsProxyTileLayer.createTile"
      to: "window.top.postMessage"
      via: "maps-plus:fetch-tile"
      pattern: "postMessage\\("
    - from: "window addEventListener('message'"
      to: "maps-plus:tile-result"
      via: "bridge inbound handler"
      pattern: "maps-plus:tile-result"
---

<objective>
Wire the iframe-side `postMessage` consumer into `DsProxyTileLayer`, add `PROXY_URL_SHAPE` to `ds-tile-proxy-helpers.js` with a drift guard against `parent-auth-bridge.js`, land Jest coverage for protocol + parent shim + blob lifecycle + `DsProxyTileLayer` bridge timing (D-02/D-06), manually replay UAT-2..UAT-7 on `splunk-10-dev`, and rebuild or commit the Webpack bundle per CLAUDE.md.

This plan satisfies **DS-AUTH-01** (iframe consumer), **DS-AUTH-02** (four-literal surface, ping/pong reserved per D-NN-2), **DS-AUTH-03** (iframe `===` origin gate), **DS-AUTH-04** (iframe-side `PROXY_URL_SHAPE` gate + drift guard vs parent file), **DS-AUTH-05** (D-NN-3 schemas + Jest structural bans on secret keys), and **DS-AUTH-06** (D-02/D-NN-6 absence behavior + automated tests + UAT replay).
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/03-ds-parent-frame-auth-bridge/03-CONTEXT.md
@.planning/phases/02-maps-plus-js-integration-testing/02-CONTEXT.md
@.planning/phases/02-maps-plus-js-integration-testing/02-UAT.md
@.planning/phases/02-maps-plus-js-integration-testing/02-UAT-MATRIX.md
@appserver/static/parent-auth-bridge.js
@appserver/static/visualizations/maps-plus/src/maps-plus.js
@appserver/static/visualizations/maps-plus/src/ds-tile-proxy-helpers.js
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parent window ↔ DS iframe | `postMessage` structured-clone payloads (no cookies) |
| Iframe bridge → Leaflet | `createTile` / `done` — must not throw into DS adapter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T03-02-01 | Spoofing | Inbound `tile-result` | mitigate | `event.origin === _SPLUNK_ORIGIN` exact equality (D-NN-4); Jest negative matrix. |
| T03-02-02 | Tampering | Malformed tile URL | mitigate | Optional `PROXY_URL_SHAPE.test(url)` before postMessage (same export as parent, D-04). |
| T03-02-03 | Info disclosure | Request/response bodies | mitigate | D-NN-3 key sets only; Jest `JSON.stringify` must not contain `cookie`, `session`, `token`, `csrf`, `password` substrings on fixtures. |
| T03-02-04 | DoS | Timers / in-flight work | mitigate | D-06 cap + timeout; `removeEventListener` in `onRemove`; D-03 revoke on both img paths. |
| T03-02-05 | Elevation | Upstream tile hosts | inherit | Phase 1 SSRF (`01-SECURITY.md`); D-NN-5 no client allow-list duplication. |
| T03-02-06 | Tampering | Rogue `type` literals | mitigate | Iframe only handles inbound `maps-plus:tile-result` for tile bytes; outbound only `maps-plus:fetch-tile` (D-NN-2). |
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task T03-02-W1: Export PROXY_URL_SHAPE + drift guard (D-04)</name>
  <read_first>
    - appserver/static/parent-auth-bridge.js
    - appserver/static/visualizations/maps-plus/src/ds-tile-proxy-helpers.js
    - appserver/static/visualizations/maps-plus/tests/ds-tile-proxy-helpers.test.js
  </read_first>
  <files>
    appserver/static/visualizations/maps-plus/src/ds-tile-proxy-helpers.js
    appserver/static/visualizations/maps-plus/tests/parent-bridge-drift.test.js
  </files>
  <behavior>
    - `tests/parent-bridge-drift.test.js` reads both files via `fs.readFileSync`, slices inclusive from the line starting `// BEGIN_PROXY_URL_SHAPE` through the line starting `// END_PROXY_URL_SHAPE`, `expect(iframeBlock).toBe(parentBlock)`.
    - `buildTileProxyUrl('https://splunk.test/en-US/splunkd/__raw/services', ...)` and `buildTileProxyUrl('/services', ...)` outputs both satisfy `PROXY_URL_SHAPE.test(...) === true`.
  </behavior>
  <action>
    Insert the **byte-identical** marked block from `appserver/static/parent-auth-bridge.js` (including `// BEGIN_PROXY_URL_SHAPE` … `// END_PROXY_URL_SHAPE` lines) into `ds-tile-proxy-helpers.js` above `module.exports`, defining `var PROXY_URL_SHAPE = /.../;` between the markers.

    Add `PROXY_URL_SHAPE` to the exports object: `PROXY_URL_SHAPE: PROXY_URL_SHAPE` (or assign after exports object creation matching file style).

    Create `tests/parent-bridge-drift.test.js` implementing `<behavior>`.
  </action>
  <verify>
    <automated>cd appserver/static/visualizations/maps-plus && npm install --ignore-scripts && npx jest tests/parent-bridge-drift.test.js tests/ds-tile-proxy-helpers.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `test $(grep -c BEGIN_PROXY_URL_SHAPE appserver/static/visualizations/maps-plus/src/ds-tile-proxy-helpers.js) -eq 1` exits 0
    - `cd appserver/static/visualizations/maps-plus && npx jest tests/parent-bridge-drift.test.js tests/ds-tile-proxy-helpers.test.js` exits 0
  </acceptance_criteria>
  <done>
    Drift guard fails CI if parent/helpers marked blocks diverge; both sample URLs match regex.
  </done>
</task>

<task type="auto">
  <name>Task T03-02-W2: DsProxyTileLayer.createTile + bridge state machine (D-02, D-03, D-06, D-NN-3, D-NN-6)</name>
  <read_first>
    - appserver/static/visualizations/maps-plus/src/maps-plus.js (AMD `define([...], function (...) {` — count parameters vs dependency array positions)
    - appserver/static/visualizations/maps-plus/src/maps-plus.js (lines 88–187)
    - appserver/static/visualizations/maps-plus/node_modules/leaflet/src/layer/tile/TileLayer.js (`createTile`, `done` contract)
    - appserver/static/visualizations/maps-plus/src/ds-tile-proxy-helpers.js (`buildTileProxyUrl`, `PROXY_URL_SHAPE`)
    - .planning/phases/03-ds-parent-frame-auth-bridge/03-CONTEXT.md (D-02, D-NN-6 — non-negotiable)
  </read_first>
  <files>
    appserver/static/visualizations/maps-plus/src/maps-plus.js
    appserver/static/visualizations/maps-plus/tests/ds-proxy-tile-layer.test.js
  </files>
  <action>
    **Step A — test scaffold (same task, enables W2 verify):** Create `tests/ds-proxy-tile-layer.test.js` covering (1) **D-CRITICAL-3 ordering:** `fs.readFileSync` of `src/maps-plus.js`; assert source index of comment `// BRIDGE_PHASE03:inflight-cap-passed` &lt; index of `// BRIDGE_PHASE03:url-shape-passed` &lt; index of `// BRIDGE_PHASE03:postmessage-send` &lt; index of `// BRIDGE_PHASE03:arm-first-fetch-timer` (strict `&lt;` chain). (2) **D-02 false-latch guard:** `jest.useFakeTimers()` integration test that simulates first `createTile` path where URL-shape fails (inject mock layer or call exported test harness if you add `module.exports.__test...` — **prefer** reading the file after edit and asserting the four `BRIDGE_PHASE03:*` markers appear in the required order **and** that the substring `arm-first-fetch-timer` does not occur earlier than `url-shape-passed` using indexOf). (3) **D-06:** assert monotonic `requestId` and 8000/256 constants appear in source or exercise helpers. (4) **D-NN-6 two-layer one-warn:** Jest spies `console.warn`; document test plan: instantiate two **mock** layer objects that share the same module-level latch functions (or integration test after implementation) and assert `console.warn` with `[maps-plus:ds-proxy] parent bridge absent — tiles disabled` is called **exactly once** when both trigger absent latch (executor implements after Step B).

    **Step B — module-level iframe-global latch (D-NN-6, D-02):** Near `_SPLUNK_ORIGIN` / `DsProxyTileLayer` (still at **module scope**, **outside** `L.TileLayer.extend({ ... })`), declare:
    - `var _bridgeLatchThisIframe = 'unknown'`  // `'unknown' | 'present' | 'absent'`
    - `var _bridgeAbsentLoggedThisIframe = false`
    - `var _firstFetchTimerThisIframe = null`  // id from first **successful** postMessage arm only
    - `var _timeoutLoggedThisIframe = false`  // first-timeout log suppression (D-06) — **iframe-global** like absent warn

    **Per-layer instance** fields remain only for `_nextRequestId`, `_inflight` (per D-06 spec: "256 in-flight **per DsProxyTileLayer instance**"), `_pendingByRequestId`, `_onParentTileMessage` binding.

    **Rule:** When `_bridgeLatchThisIframe === 'absent'`, **every** `DsProxyTileLayer` instance returns blank `img` synchronously with **no** `postMessage` and **no** absent warn (warn already fired once).

    In `DsProxyTileLayer` extend literal, add **`createTile`** and **`onRemove`** (chain `L.TileLayer.prototype.onRemove.call(this)`).

    **Initialize:** after super `initialize`, `window.addEventListener('message', this._onParentTileMessage, false)` with bound handler.

    **`createTile` order (D-CRITICAL-3 — do not reorder):**
    1. `var img = document.createElement('img')`, `var splunkOrigin = _SPLUNK_ORIGIN`.
    2. If `splunkOrigin === ''`: set `_bridgeLatchThisIframe = 'absent'`; if `!_bridgeAbsentLoggedThisIframe` then `console.warn('[maps-plus:ds-proxy] parent bridge absent — tiles disabled')` and `_bridgeAbsentLoggedThisIframe = true`; assign transparent GIF to `img.src`; `// BRIDGE_PHASE03:url-shape-passed` **must not** be reached — skip markers by branching; `return img` and `done(null, img)`.
    3. If `_bridgeLatchThisIframe === 'absent'`: transparent GIF, `done(null, img)`, **no** postMessage, **no** warn.
    4. `var url = this.getTileUrl(coords)`.
    5. **Inflight cap** check **first** among send preconditions: if `this._inflight >= 256`, transparent GIF + `done(null, img)`; insert comment `// BRIDGE_PHASE03:inflight-cap-passed` only on the code path that continues past the cap (i.e. place marker immediately **after** the early-return cap block).
    6. URL-shape: if `DsTileProxyHelpers.PROXY_URL_SHAPE && !DsTileProxyHelpers.PROXY_URL_SHAPE.test(url)` then transparent GIF + `done(null, img)` — **no postMessage, do not arm 1500ms timer**; insert `// BRIDGE_PHASE03:url-shape-passed` on the **continuing** path after the shape gate (immediately after the closing brace of the reject branch).
    7. Increment `this._inflight`, allocate `requestId`, store pending + **8000ms** timer per instance rules.
    8. `// BRIDGE_PHASE03:postmessage-send` comment line immediately before `window.top.postMessage(...)`.
    9. **Only here**, if `_bridgeLatchThisIframe === 'unknown'` and `_firstFetchTimerThisIframe === null`, set `_firstFetchTimerThisIframe = setTimeout(function () { ... }, 1500)` with callback that sets `_bridgeLatchThisIframe = 'absent'`, resolves pendings, and applies the **single** `_bridgeAbsentLoggedThisIframe` warn path. Place comment `// BRIDGE_PHASE03:arm-first-fetch-timer` on the line **immediately before** `setTimeout`.
    10. `postMessage` payload and target origin per prior plan text (`z,x,y` from coords, optional `s`/`r` from parsed `url`).

    **`_onParentTileMessage`:** On first valid `tile-result` while `_bridgeLatchThisIframe === 'unknown'`, set `_bridgeLatchThisIframe = 'present'`, `clearTimeout(_firstFetchTimerThisIframe)`, `_firstFetchTimerThisIframe = null`.

    **All failure paths:** use **`done(null, img)` only** — never `done(new Error(...), img)`. On image `onerror` after Blob URL, still `done(null, img)` after revoke. Comment: "Leaflet `tileerror` is already wired in `_createMapsPlusTileLayer`; blank tile + `done(null, img)` avoids bubbling errors into the DS adapter."

    **DS-AUTH-02:** only four `maps-plus:*` type literals in `maps-plus.js` edits.

    **`onRemove`:** `window.removeEventListener('message', this._onParentTileMessage, false)` then super.
  </action>
  <verify>
    <automated>cd appserver/static/visualizations/maps-plus && npm install --ignore-scripts && npx jest tests/ds-proxy-tile-layer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "_bridgeLatchThisIframe" appserver/static/visualizations/maps-plus/src/maps-plus.js` is ≥ 3
    - `grep -c "_bridgeAbsentLoggedThisIframe" appserver/static/visualizations/maps-plus/src/maps-plus.js` is ≥ 2
    - `test $(grep -n "var _bridgeLatchThisIframe" appserver/static/visualizations/maps-plus/src/maps-plus.js | head -1 | cut -d: -f1) -lt $(grep -n "DsProxyTileLayer = L.TileLayer.extend" appserver/static/visualizations/maps-plus/src/maps-plus.js | head -1 | cut -d: -f1)` exits 0 (module-level latch declared **before** the `DsProxyTileLayer` extend — not `this._bridgeLatch` inside the literal)
    - `grep -c "// BRIDGE_PHASE03:inflight-cap-passed" appserver/static/visualizations/maps-plus/src/maps-plus.js` is ≥ 1 and same for `url-shape-passed`, `postmessage-send`, `arm-first-fetch-timer`
    - `grep -c "done(new Error" appserver/static/visualizations/maps-plus/src/maps-plus.js` returns **0** (no `done(new Error` anywhere in file after edit — MAJOR-5)
    - `cd appserver/static/visualizations/maps-plus && npx jest tests/ds-proxy-tile-layer.test.js` exits 0
    - `node -e "const fs=require('fs');const s=fs.readFileSync('appserver/static/visualizations/maps-plus/src/maps-plus.js','utf8');const i=s.indexOf('// BRIDGE_PHASE03:inflight-cap-passed');const u=s.indexOf('// BRIDGE_PHASE03:url-shape-passed');const p=s.indexOf('// BRIDGE_PHASE03:postmessage-send');const t=s.indexOf('// BRIDGE_PHASE03:arm-first-fetch-timer');if(i<0||u<0||p<0||t<0)process.exit(1);if(!(i<u&&u<p&&p<t))process.exit(1);"` run from repo root exits 0
  </acceptance_criteria>
  <done>
    DS-only `createTile` path implemented; module-global absent latch + timer arm order; `ds-proxy-tile-layer.test.js` green.
  </done>
</task>

<task type="auto">
  <name>Task T03-02-W3: Jest — shim vm harness, iframe protocol negatives, blob revoke symmetry</name>
  <read_first>
    - appserver/static/visualizations/maps-plus/jest.config.js
    - appserver/static/visualizations/maps-plus/package.json
    - appserver/static/parent-auth-bridge.js
    - appserver/static/visualizations/maps-plus/tests/ds-tile-proxy-helpers.test.js
    - CLAUDE.md (D-22 `npm run build` + `visualization.js` commit rules)
  </read_first>
  <files>
    appserver/static/visualizations/maps-plus/package.json
    appserver/static/visualizations/maps-plus/tests/parent-bridge-rpc-iframe.test.js
    appserver/static/visualizations/maps-plus/tests/parent-bridge-shim.test.js
    appserver/static/visualizations/maps-plus/tests/parent-bridge-blob-lifecycle.test.js
  </files>
  <action>
    Add **`jest-environment-jsdom`** `^29.7.0` to `devDependencies` in `package.json`. Run `npm install --ignore-scripts`.

    **`tests/parent-bridge-rpc-iframe.test.js` (node):** Implement pure tests that:
    - Build sample **request** objects and assert only keys `type,requestId,url,z,x,y` plus optional `s,r` are present for allowed fixtures; assert `JSON.stringify` of each fixture does not include substrings `cookie`, `session`, `token`, `csrf`, `password` (case-sensitive is enough).
    - Assert origin gate: `function allowOrigin(expected, candidate) { return candidate === expected }` rejects `expected + '/'`, `expected.replace(':8000', ':8443')` when lengths differ, and rejects `https://splunkweb-evil.example.com` when `expected === 'https://splunkweb.example.com'`.

    **`tests/parent-bridge-shim.test.js` (node + `vm`):** Read `appserver/static/parent-auth-bridge.js` into a string. Create a `fakeWindow` object with `location: { origin: 'https://splunk.test' }`, `addEventListener(ev, fn)` storing `global.__parentHandler = fn`, `console: { info() {}, warn(w) { (global.__warns = global.__warns || []).push(String(w)) } }`, `fetch: jest.fn(() => Promise.resolve({ ok: true, status: 200, headers: { get: () => 'image/png' }, blob: () => Promise.resolve(new Blob([Uint8Array.of(137)], { type: 'image/png' })) }))`, and `__MAPS_PLUS_PARENT_BRIDGE__` guard field reset to `undefined` before each test. Use `vm.runInNewContext(wrappedCode, { window: fakeWindow, console: fakeWindow.console, fetch: fakeWindow.fetch, URL, Blob, Uint8Array, TextEncoder, Map, Date, setTimeout, clearTimeout })` where `wrappedCode` assigns `global.window = window` then appends the parent file (strip outer `"use strict"` from parent if present — parent has none). Invoke the stored handler with `new MessageEvent('message', { data: { type: 'maps-plus:fetch-tile', requestId: 1, url: <valid proxy url matching regex under test>, z: 0, x: 0, y: 0 }, origin: 'null', source: { postMessage: jest.fn() } })` — use **real** `MessageEvent` from `require('node:events')` **not** available — construct plain object `{ type:'message', data, origin:'null', source }` and call `handler(eventLike)`.

    Fire **501** sequential messages with unique `requestId` and valid URL from `require('../src/ds-tile-proxy-helpers').buildTileProxyUrl('https://splunk.test/en-US/splunkd/__raw/services','https://tile/{z}/{x}/{y}.png',{z:0,x:0,y:0})` asserting `fakeWindow.fetch.mock.calls.length === 500` and **one** warn includes substring `[maps-plus:parent-bridge] rate limit exceeded for iframe origin=null`.

    **Parent handler D-NN-4 negative origins (MAJOR-2):** In the **same** `parent-bridge-shim.test.js` vm harness, add **≥4** `it('parent handler: reject origin …', …)` cases that dispatch an otherwise-valid `maps-plus:fetch-tile` payload through the real parent `message` handler but with `event.origin` set to each of: (1) `https://splunk.test` (wrong — must be serialized `null` only), (2) `https://splunk.test/` (trailing slash), (3) `https://splunk.test:8443` (port drift), (4) `https://splunkweb-evil.example.com` (subdomain variant). Each test asserts **`fetch` was not called** and **`source.postMessage` was not called**.

    **`tests/parent-bridge-blob-lifecycle.test.js`:** File starts with docblock `/** @jest-environment jsdom */`. Create `new Image()`, assign `onload`/`onerror` handlers using the **exact** D-03 pattern from `03-CONTEXT.md` (`URL.createObjectURL`, set `src`, revoke on both events). Use `jest.spyOn(URL, 'createObjectURL')` and `jest.spyOn(URL, 'revokeObjectURL')`. Dispatch `load` via `Object.defineProperty` or `img.dispatchEvent(new Event('load'))` after assigning `src` to a blob URL; assert `revokeObjectURL` call count equals `createObjectURL` call count (`toBe(1)` each).

    **Literal grep budget (DS-AUTH-02):** Add one test that reads `maps-plus.js` text with `fs.readFileSync` and regex-counts `maps-plus:[a-z-]+` matches — assert set equals exactly the four allowed literals (implement by extracting quoted strings containing `maps-plus:`).

    **D-22 bundle (merged from former Task W5):** After **all** Jest suites below exit 0, run `npm run build` in `appserver/static/visualizations/maps-plus/`. Stage and commit **`visualization.js`** and **`visualization.css`** together with the Phase 3 source/test edits in one atomic commit (no `node_modules/`). Executor verifies `git show --name-only --pretty=format: HEAD | grep -E 'visualization\\.(js|css)$'` lists both files on that commit.
  </action>
  <verify>
    <automated>cd appserver/static/visualizations/maps-plus && npm install --ignore-scripts && npx jest tests/parent-bridge-drift.test.js tests/parent-bridge-rpc-iframe.test.js tests/parent-bridge-shim.test.js tests/parent-bridge-blob-lifecycle.test.js tests/ds-tile-proxy-helpers.test.js tests/ds-proxy-tile-layer.test.js</automated>
  </verify>
  <acceptance_criteria>
    - `cd appserver/static/visualizations/maps-plus && npx jest tests/parent-bridge-drift.test.js tests/parent-bridge-rpc-iframe.test.js tests/parent-bridge-shim.test.js tests/parent-bridge-blob-lifecycle.test.js tests/ds-tile-proxy-helpers.test.js tests/ds-proxy-tile-layer.test.js` exits 0
    - `grep -q "jest-environment-jsdom" appserver/static/visualizations/maps-plus/package.json` exits 0
    - `grep -q "@jest-environment jsdom" appserver/static/visualizations/maps-plus/tests/parent-bridge-blob-lifecycle.test.js` exits 0
    - `grep -q "vm.runInNewContext" appserver/static/visualizations/maps-plus/tests/parent-bridge-shim.test.js` exits 0
    - `test $(grep -cE "it\\('parent handler: reject origin" appserver/static/visualizations/maps-plus/tests/parent-bridge-shim.test.js) -ge 4` exits 0
    - `grep -q "splunkweb-evil" appserver/static/visualizations/maps-plus/tests/parent-bridge-rpc-iframe.test.js` exits 0
    - `grep -c "maps-plus:fetch-tile" appserver/static/visualizations/maps-plus/visualization.js` is ≥ 1 (post-build bundle contains bridge string — D-22 evidence)
  </acceptance_criteria>
  <done>
    Automated tests cover drift, shim rate limit + fetch + parent origin negatives, iframe protocol negatives, blob revoke symmetry, maps-plus literal surface, `ds-proxy-tile-layer` ordering; Webpack bundle rebuilt and committed.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task T03-02-W4: Manual UAT-2 through UAT-7 replay on splunk-10-dev</name>
  <read_first>
    - .planning/phases/02-maps-plus-js-integration-testing/02-UAT.md
    - .planning/phases/02-maps-plus-js-integration-testing/02-UAT-MATRIX.md
  </read_first>
  <files>
    .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md
  </files>
  <action>
    Deploy to `splunk-10-dev`. Execute **UAT-2, UAT-3, UAT-4, UAT-5, UAT-6, UAT-7** per `02-UAT.md`. For **UAT-2**, confirm DS tiles return **200** image responses when shim loads; confirm absence case prints exactly **one** console line containing `[maps-plus:ds-proxy] parent bridge absent — tiles disabled` per dashboard session when shim intentionally disabled.

    Append `03-02-SUMMARY.md` with section `## UAT replay (UAT-2..UAT-7)` and a markdown table `UAT ID | Result | Notes` including all seven identifiers as separate rows.

    On the line **immediately under** the `## UAT replay` heading (or as the first line inside that section), record the Phase 02 matrix row mapping verbatim:
    `MATRIX:UAT-2→DS-1,DS-2,DS-3;UAT-3→DS-5,DS-6;UAT-4→P-9;UAT-5→DS-8;UAT-6→E-1,E-2;UAT-7→E-4`
    (IDs taken from `.planning/phases/02-maps-plus-js-integration-testing/02-UAT-MATRIX.md` — Section 2 DS rows, Section 3 provider sweep, Section 4 edge cases).
  </action>
  <verify>
    <automated>test -f .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md && grep -q '## UAT replay' .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `for u in 2 3 4 5 6 7; do grep -q "UAT-$u" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md || exit 1; done` exits 0
    - `grep -q "MATRIX:UAT-2→DS-1,DS-2,DS-3" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
    - `grep -q "UAT-3→DS-5,DS-6" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
    - `grep -q "UAT-4→P-9" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
    - `grep -q "UAT-5→DS-8" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
    - `grep -q "UAT-6→E-1,E-2" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
    - `grep -q "UAT-7→E-4" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
    - `grep -q "splunk-10-dev" .planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exits 0
  </acceptance_criteria>
  <done>
    Human recorded UAT replay outcomes in SUMMARY.
  </done>
</task>

</tasks>

<verification>
- `cd appserver/static/visualizations/maps-plus && npm install --ignore-scripts && npx jest`
- `cd appserver/static/visualizations/maps-plus && npm run build`
- `grep -E 'maps-plus:(fetch-tile|tile-result|ping|pong)' appserver/static/parent-auth-bridge.js appserver/static/visualizations/maps-plus/src/maps-plus.js`
</verification>

<success_criteria>
Jest green; DS bridge works in UAT-2; absence + timeout hygiene matches locked strings; bundle committed; Classic unchanged.
</success_criteria>

<output>
After completion, ensure `.planning/phases/03-ds-parent-frame-auth-bridge/03-02-SUMMARY.md` exists (seeded by Task W4).
</output>
